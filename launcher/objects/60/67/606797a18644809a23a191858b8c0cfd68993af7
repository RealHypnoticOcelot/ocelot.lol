{
	// Config for time buttons
	"time": {
		// First add button (default: 1000). [Values: any number]
		"addButton1": 1000,
		// Second add button (default: 10000). [Values: any number]
		"addButton2": 10000
	},
	// Config for weather buttons
	"weather": {
		// Delay for how long a weather condition lasts (ticks). [Values: any number]
		"delay": 1500
	}
}